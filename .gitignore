node_modules/
.env
uploads/*
data/*.json
!data/.gitkeep
.aider*

# Global gitignore patterns like "Public/" match "public/" on case-insensitive
# filesystems and would skip our web UI — force-track these paths.
!/public/
!/public/**
